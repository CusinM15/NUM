syms x
format long
f = @(x) 5*cos(x - exp(x)) - x;
% a = 0;
% b = 1;
% for i = 1:4
%     k = (f(a) - f(b))/(a - b);
%     n = f(a) - k * a;
%     sekanta = k * x + n;
%     c = -n / k;
%     if f(a) * f(c) < 0
%         b = c;
%     else
%         a = c;
%     end
% end
% c
a = 1
b = 2
% for i = 1:4
%     k = (f(a) - f(b))/(a - b);
%     n = f(a) - k * a;
%     sekanta = k * x + n;
%     c = -n / k;
%     if f(a) * f(c) < 0
%         b = c;
%     else
%         a = c;
%     end
% end
while
    